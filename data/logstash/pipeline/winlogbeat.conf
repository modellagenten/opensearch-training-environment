input {
  beats {
    port => 5044
  }
}

output {
  opensearch {
    hosts       => ["https://opensearch-node1:9200", "https://opensearch-node2:9200"]
    user        => "admin"
    password    => "${OPENSEARCH_PASSWORD}"
    index       => "winlogbeat-stream"
    ssl_certificate_verification => false
    action => "create"
  }
}
